#include "ui/ui_slots.h"

#include "esp_log.h"

static const char *TAG = "ui_slots";

void ui_slots_create(void)
{
    ESP_LOGI(TAG, "Creating slot overview (stub)");
}

void ui_slots_refresh(void)
{
    ESP_LOGD(TAG, "Refreshing slots view");
}
